// only needed to avoid compilation issues in stackblitz!
{
  "compilerOptions": {
    "target": "esnext",
    "module": "esnext",
    "moduleResolution": "node",
    "lib": ["esnext"],
    "sourceMap": true,
    "inlineSources": true,
    "experimentalDecorators": true
  }
}
